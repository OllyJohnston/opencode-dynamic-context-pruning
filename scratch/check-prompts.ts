import { SYSTEM } from "../lib/prompts/system"
import { COMPRESS_RANGE } from "../lib/prompts/compress-range"

console.log("SYSTEM prompt length:", SYSTEM.length)
console.log("COMPRESS_RANGE prompt length:", COMPRESS_RANGE.length)
console.log("Successfully imported prompts.")
